on:
  push:
    branches:
      - main

jobs:
  release-drafter:
    runs-on: ubuntu-20.04
    steps:
      - uses: release-drafter/release-drafter@df69d584deac33d8569990cb6413f82447181076 # tag=v5
        env:
          GITHUB_TOKEN: ${{ secrets.CR_PAT }}